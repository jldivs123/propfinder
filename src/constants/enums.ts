// * Based from https://mui.com/material-ui/customization/breakpoints/
export enum BREAKPOINTS {
  XS = 0,
  SM = 600,
  MD = 900,
  LG = 1200, // * value here is used as the minimum, while on the other they are used as maximum
  XL = 1536,
}

export enum HttpMethod {
  GET = "GET",
  PATCH = "PATCH",
  POST = "POST",
  PUT = "PUT",
  DELETE = "DELETE",
}

export enum PrecisionLevels {}
